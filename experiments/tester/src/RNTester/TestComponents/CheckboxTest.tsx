import * as React from 'react';
import { Checkbox } from 'react-native-uifabric';

export const CheckboxTest: React.FunctionComponent<{}> = () => {
  return <Checkbox label="This is a test checkbox" />;
};
